// algunes funcions amb reals de la llibreria matemàtica

#include <cmath>
#include <iostream>
using namespace std;

int main()
{
    double x, y;
    cin >> x >> y;
    cout << sin(x) << " " << cos(x) << " " << pow(x, y) << endl;
}
